# Archive

`originals/` contains unchanged February 2026 HTML prototypes retained for provenance.

They contain useful earlier analysis and code, but they also contain the classification, timing, calibration and MCTS issues documented in the root [`METHOD.md`](../METHOD.md). Use the root `index.html` as the current experiment surface.

